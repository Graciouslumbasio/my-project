def get_grade(marks):
    return 'A' if marks >= 90 else 'B' if marks >= 80 else 'C' if marks >= 70 else 'D' if marks >= 60 else 'F'

name = input("Enter student name: ")
marks = float(input("Enter marks: "))
print(f"{name} received grade {get_grade(marks)}.")

students = {}
for _ in range(3):
    name = input("Name: ")
    marks = float(input("Marks: "))
    students[name] = 'A' if marks >= 90 else 'B' if marks >= 80 else 'C' if marks >= 70 else 'D' if marks >= 60 else 'F'
print(students)
